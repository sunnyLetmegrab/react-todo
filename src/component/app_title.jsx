import React from 'react'

export default function AppTitle({ className }) {
     return (
        <div className={`block content-center font-pacifico text-[#1F3E97] text-2xl ${className}`}>vantela</div>
    )
}
